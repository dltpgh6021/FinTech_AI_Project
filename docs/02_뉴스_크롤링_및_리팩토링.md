# Day 2. 네이버 경제 뉴스 크롤링 및 리팩토링

 > TIL (Today I Learned)
 > - 크롤링 하는 방법
 > - 크롤링한 내용 csv 파일로 정리
 > - 리팩토링을 통해 함수 형태로 저장

 ## 크롤링 하는 방법
 ```py
 import requests
from bs4 import BeautifulSoup

# 1. 타겟 주소 (삼성전자 뉴스 검색)
url = "https://search.naver.com/search.naver?where=news&query=삼성전자"

# 2. 브라우저 위장
headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'}

# 3. 데이터 가져오기 및 요리 준비
response = requests.get(url, headers=headers)
soup = BeautifulSoup(response.text, 'html.parser')

# 4. 네가 찾아낸 정확한 이름표 적용!
# span 태그 중에서 클래스 이름이 'sds-comps-text-type-headline1'인 녀석들만 싹 다 찾아와!
titles = soup.select('span.sds-comps-text-type-headline1')

# 5. 깔끔하게 텍스트만 뽑아서 출력
print("--- 📈 삼성전자 최신 뉴스 TOP 10 ---")

if titles:
    for i, title in enumerate(titles[:10]):
        # get_text()가 <mark> 태그까지 알아서 무시하고 글자만 쏙 빼줍니다.
        clean_title = title.get_text().strip()
        print(f"{i+1}. {clean_title}")
else:
    print("🚨 앗, 아직도 못 찾았어요. 코드를 다시 확인해 주세요.")
 ```

맨 처음 import한 requests와 BeautifulSoup는 다음과 같은 역할을 한다.

requests : 웹 사이트에서 데이터를 가져와주는 역할 <br>
bs4 (BeautifulSoap) : requests에서 가져온 데이터를 읽기 쉬운 형태로 바꿔주는 역할

url에 데이터를 가져오고자 하는 원래 주소를 입력한다. <br>
그리고, headers에는 User-Agent를 넣어준다. 이는 크롤링 해가는 주체가 봇이 아니라는 일종의 신분증 역할을 한다. 그래서 자동으로 막아주는 벽을 뚫을 수 있게 해준다. 

그 다음으로 requests.get 함수로 데이터를 가져오고, BeautifulSoup(response.text, 'html.parser') 함수로 내가 처리하기 편한 사이즈로 컷해준다. 

soup.select 함수를 이용하여 내가 선택한 태그가 있는 문장만 선별한다. <br>
여기서 내가 원하는 태그는 제목 태그이며, 웹 사이트에 들어가 F12를 통해 태그를 직접 확인하여 가져와야 한다. 

이후 get_text함수를 통해 글자만 쏙 빼와주고 내가 가져온 제목들을 출력한다. 

 ## 크롤링한 내용 csv 파일로 정리
```py
import pandas as pd

# 1. 뉴스 제목을 담을 리스트 준비
news_list = []

# 2. 추출한 제목들을 바구니에 담기
for title in titles[:10]:
    clean_title = title.get_text().strip()
    news_list.append(clean_title)

# 3. 바구니에 담긴 데이터를 DataFrame로 변환
df_news = pd.DataFrame(news_list, columns=['Title'])

# 4. 내 컴퓨터에 csv로 저장
# utf-8-sig를 넣어야 엑셀로 열었을 때 한글이 외계어로 안깨짐
df_news.to_csv('samsung_news.csv', index=False, encoding='utf-8-sig')

print('samsung_news.csv 저장 완료')

# 5. 주피터 화면에서 head 출력
df_news.head()
```

저장은 비교적 간단하다. 

우선 제목들을 list에 저장하고, 이들을 DataFrame으로 바꿔준다. 

이후 이 DataFrame을 csv로 저장한다. 

여기서 중요한 점. encoding을 utf-8-sig로 해야 엑셀로 열 때 한글이 안깨진다. 


 ## 리팩토링을 통해 함수 형태로 저장

리팩토링을 통해 함수를 리뷰하고 재사용가능하게 바꿔준다. 

./src/data_collector.py에 크롤링 리팩토링 파일이 있다. <br>
함수 구조는 위에서 설명한 크롤링 하는 방법과 동일하다. 

추가한 것은 예외처리를 한 것 정도? 

파일의 맨 아래 보면 다음과 같은 코드가 적혀있다. 
```py
if __name__ == "__main__":
    print("--- data_collector.py 모듈 테스트 시작 ---")
    df_samsung = get_naver_news('삼성전자', 5)
    print(df_samsung)
```

이는 리팩토링 함수 파일에서 매우 중요한 역할을 한다. 

다른 파일에서 import data_collector를 할 때에는 조용히 함수만 빌려준다. <br>
하지만, 파일 자체를 실행시킬 때는 테스트용 코드가 작동하게 하는 중요한 구조이다. <br>
C언어의 main 함수 역할을 한다고 생각하면 된다. 